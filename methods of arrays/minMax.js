/**
 * Функция, которая принимает на вход массив чисел и возвращает наименьшее и наибольшее число в массиве.
 * @param {number[]} arr - входной массив чисел
 * @return {number[]} - массив, содержащий наименьшее и наибольшее число в массиве
 */
function minMax(arr) {
    // ваш код здесь
    if (arr.length === 0) return [];
    
    const min = Math.min(...arr);
    const max = Math.max(...arr);

    return [min, max];
}

/*
if (arr.length === 0) return []; — если массив пустой, возвращаем пустой 
массив, так как нет значений для сравнения.

Math.min(...arr) — метод Math.min() принимает не массив, а список 
аргументов, поэтому используем оператор spread (...), чтобы развернуть 
массив в отдельные аргументы.

Math.max(...arr) — аналогично, метод Math.max() для нахождения 
наибольшего числа.

return [min, max]; — возвращаем новый массив, содержащий минимальное и 
максимальное значение.
*/
